# Empty stub module — preserved because removing the corresponding
# tabModule Def row requires a destructive DB write that needs explicit
# authorization (see Phase J deviation note in the build report).
# All integration code lives under ecommerce_super.easyecom.* instead.
